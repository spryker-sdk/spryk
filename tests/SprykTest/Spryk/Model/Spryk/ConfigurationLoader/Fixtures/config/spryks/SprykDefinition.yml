arguments:
  emptyValue: ~

  replaceValue:
    value: "%module%/src/"

  replaceDefault:
    default: "%module%/src/"

postSpryks:
  - FooBarSpryk:
      arguments:
        replaceValue:
            value: "%module%/src/foo-bar/"

        replaceDefault:
          default: "%module%/src/foo-bar/"
