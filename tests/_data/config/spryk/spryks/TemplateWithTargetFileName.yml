spryk: template
description: "Unit Test configuration"

arguments:
  mode:
      value: core

  module: ~

  template:
    value: composer.json.twig

  targetFilename:
    value: new-filename.json

  targetPath:
    value: "{{ module }}/"
