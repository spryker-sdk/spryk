spryk: template
description: "Unit Test configuration"
mode: both

arguments:
  mode: ~

  module: ~

  template:
    value: composer.json.twig

  targetPath:
    default: "{{ module }}/"

  targetFilename:
      value: composer.json

preSpryks:
  - AddModule
