spryk: template
description: "Unit Test configuration"

arguments:
  mode:
      value: core

  module: ~

  template:
    value: composer-with-filter.json.twig

  targetPath:
    value: "{{ module }}/"
