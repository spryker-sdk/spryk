spryk: template
description: "Unit Test configuration"

arguments:
  moduleName:
    value: FooBar

  organization:
    value: Spryker

  template:
    value: FacadeInterface.php.twig

  targetFilename:
    value: "{{ moduleName }}FacadeInterface.php"

  targetPath:
    value: "{{ moduleName }}/src/{{ organization }}/Zed/{{ moduleName }}/Business/"
