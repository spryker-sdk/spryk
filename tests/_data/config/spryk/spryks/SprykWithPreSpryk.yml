spryk: template
description: "Unit Test configuration"

arguments:
  mode:
      value: core
  module:
    value: FooBar

  template:
    value: README.md.twig

  targetPath:
    value: "{{ module }}/"

preSpryks:
  - StructureWithoutInteraction
