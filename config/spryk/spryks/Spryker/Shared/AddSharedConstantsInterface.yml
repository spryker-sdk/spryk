spryk: template
description: "Adds a new *module*ConstantsInterface to the Shared application."

arguments:
  module: ~

  organization:
    default: Spryker

  template:
    value: SharedConstantsInterface.php.twig

  targetFilename:
    value: "{{ module }}Constants.php"

  targetPath:
    default: "{{ module }}/src/{{ organization }}/Shared/{{ module }}/"

preSpryks:
  - CreateSprykerModule
