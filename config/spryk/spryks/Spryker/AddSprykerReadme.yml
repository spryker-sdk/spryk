spryk: template
description: "Adds a new README.md to the Modules root."

arguments:
  moduleName: ~

  organization:
    default: Spryker

  template:
    value: README.md.twig

  targetPath:
    default: "{{ moduleName }}/"

preSpryks:
  - AddSprykerModule
