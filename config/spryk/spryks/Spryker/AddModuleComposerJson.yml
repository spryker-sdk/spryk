spryk: template
description: "Adds a new composer.json to the Modules root."
mode: core

arguments:
  module: ~

  organization:
    default: Spryker

  template:
    value: composer.json.twig

  targetPath:
    default: "{{ module }}/"

preSpryks:
  - AddModule
