spryk: template
description: "Adds a new phpstan.json to the Modules root."

arguments:
  moduleName: ~

  organization:
    default: Spryker

  template:
    value: phpstan.json.twig

  targetPath:
    default: "{{ moduleName }}/"

preSpryks:
  - CreateSprykerModule
