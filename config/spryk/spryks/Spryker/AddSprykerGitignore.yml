spryk: template
description: "Adds a new .gitignore to the Modules root."

arguments:
  module: ~

  organization:
    default: Spryker

  template:
    value: gitignore.twig

  targetFilename:
    value: .gitignore

  targetPath:
    default: "{{ module }}/"

preSpryks:
  - CreateSprykerModule
