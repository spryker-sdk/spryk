spryk: template
description: "Adds a new phpstan.neon to the Modules root."

arguments:
  module: ~

  organization:
    default: Spryker

  template:
    value: phpstan.neon.twig

  targetPath:
    default: "{{ module }}/"

preSpryks:
  - CreateSprykerModule
