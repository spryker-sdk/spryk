spryk: template
description: "Adds a new *moduleName*Client to the Client application."
priority: 1

arguments:
  moduleName: ~

  organization:
    default: Spryker

  template:
    value: Client/Client.php.twig

  targetFilename:
    value: "{{ moduleName }}Client.php"

  targetPath:
    default: "{{ moduleName }}/src/{{ organization }}/Client/{{ moduleName }}/"

preSpryks:
  - AddSprykerModule

postSpryks:
  - AddClientDependencyProvider
  - AddClientInterface
  - AddClientFactory
