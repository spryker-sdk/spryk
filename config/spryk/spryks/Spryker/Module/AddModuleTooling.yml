spryk: template
description: "Adds a new tooling.yml to the Modules root."
mode: core

arguments:
    module:
        inherit: true

    organization:
        inherit: true
        default: Spryker

    template:
        value: tooling.yml.twig

    targetPath:
        value: ""

preSpryks:
    - AddModule
