spryk: template
description: "Adds a new LICENSE to the Modules root."
mode: core

arguments:
    module:
        inherit: true

    organization:
        inherit: true
        default: Spryker

    template:
        value: LICENSE.twig

    targetPath:
        value: ""

preSpryks:
    - AddModule
