spryk: template
description: "Adds a new LICENSE to the Modules root."

arguments:
  module: ~

  organization:
    default: Spryker

  template:
    value: LICENSE.twig

  targetPath:
    default: "{{ module }}/"

preSpryks:
  - AddSprykerModule
