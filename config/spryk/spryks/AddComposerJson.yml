spryk: template
description: "Adds a new composer.json to the Modules root."

arguments:
  module: ~

  moduleOrganization:
    default: Spryker

  template:
    value: composer.json.twig

  targetPath:
    default: "%module%/"

preSpryks:
  - CreateModule
