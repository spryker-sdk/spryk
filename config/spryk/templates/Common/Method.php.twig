
    /**
     {% if comment is defined and comment.value is not empty %}
     {%- for commentLine in comment.value -%}
     * {{ commentLine }}
     {% endfor -%}
     *
     {% endif -%}
     {% if annotations.value is not empty -%}
     {% for annotation in annotations.value -%}
     * {{ annotation }}
     *
     {% endfor -%}
     {% endif -%}
     {% if input.value is not empty -%}
     {% set inputs = input.value | split(',') -%}
     {%- if inputs is iterable -%}
     {% for input in inputs -%}
     * @param {{ input | argumentToDocParameter }}
     {% endfor -%}
     *
     {% endif -%}
     {% endif -%}
     * @return {{ output }}
     */
    public function {{ method }}({% if input.value is not empty %}{{ input.value | typedArrayConvert }}{% endif %}){% if output.value is not empty %}: {{ output | typedArrayConvert }}{% endif %}

    {
    {% if body is defined and body is not empty %}
    {{ body | raw }}
    {% endif -%}
    }
