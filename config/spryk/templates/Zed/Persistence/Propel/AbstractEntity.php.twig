<?php

{{ include('Partials/licence.twig') }}

namespace {{ organization }}\Zed\{{ module }}\Persistence\Propel;

use Orm\Zed\{{ module }}\Persistence\Base\{{ tableName | camelCased }};

/**
 * Skeleton subclass for representing a row from the '{{ tableName }}' table.
 *
 *
 *
 * You should add additional methods to this class to meet the
 * application requirements. This class will only be generated as
 * long as it does not already exist in the output directory.
 */
abstract class Abstract{{ tableName | camelCased }} extends {{ tableName | camelCased }}
{
}
