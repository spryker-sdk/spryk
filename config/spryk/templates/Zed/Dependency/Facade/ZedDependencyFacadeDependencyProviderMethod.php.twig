
    /**
     * @return \Spryker\Zed\Kernel\Container
     */
    protected function {{ method }}(\Spryker\Zed\Kernel\Container $container): \Spryker\Zed\Kernel\Container
    {
        $container[static::FACADE_{{ dependentModule | underscored | upper }}] = function (\Spryker\Zed\Kernel\Container $container) {
            ${{ moduleName | lcfirst }}To{{ dependentModule }}FacadeBridge = new \{{ organization }}\Zed\{{ moduleName }}\Dependency\Facade\{{ moduleName }}To{{ dependentModule }}FacadeBridge(
                $container->getLocator()->{{ dependentModule | lcfirst }}()->facade()
            );

            return ${{ moduleName | lcfirst }}To{{ dependentModule }}FacadeBridge;
        };

        return $container;
    }
