

    /**
     * @return \Spryker\Client\Kernel\Container
     */
    protected function {{ providerMethod }}(\Spryker\Client\Kernel\Container $container): \Spryker\Client\Kernel\Container
    {
        $container->set(static::CLIENT_{{ dependentModule | underscored | upper }}, function (\Spryker\Client\Kernel\Container $container) {
            return new \{{ organization }}\Client\{{ module }}\Dependency\Client\{{ module }}To{{ dependentModule }}ClientBridge(
                $container->getLocator()->{{ dependentModule | lcfirst }}()->client()
            );
        });

        return $container;
    }
