

    /**
     * @return \Spryker\Client\Kernel\Container
     */
    protected function {{ providerMethod }}(\Spryker\Client\Kernel\Container $container): \Spryker\Client\Kernel\Container
    {
        $container->set(static::SERVICE_{{ dependentModule | underscored | upper }}, function (\Spryker\Client\Kernel\Container $container) {
            return new \{{ organization }}\Client\{{ module }}\Dependency\Service\{{ module }}To{{ dependentModule }}ServiceBridge(
                $container->getLocator()->{{ dependentModule | lcfirst }}()->service()
            );
        });

        return $container;
    }
