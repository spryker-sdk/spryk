

    /**
     * Specification:
     {% set specification = specification | array %}
     {%- for specificationLine in specification -%}
     * - {{ specificationLine }}
     {% endfor -%}
     *
     * @api
     {% set inputs = input.value | split(',') -%}
     {%- if inputs is iterable -%}
     *
     {% for input in inputs -%}
     * @param {{ input }}
     {% endfor -%}
     *
     {% endif -%}
     * @return {{ output }}
     */
    public function {{ method }}({{ input }}): {{ output }};
