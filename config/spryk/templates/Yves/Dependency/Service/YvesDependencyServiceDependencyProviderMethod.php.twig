
    /**
     * @return \Spryker\Yves\Kernel\Container
     */
    protected function {{ method }}(\Spryker\Yves\Kernel\Container $container): \Spryker\Yves\Kernel\Container
    {
        $container[static::SERVICE_{{ dependentModule | underscored | upper }}] = function (\Spryker\Yves\Kernel\Container $container) {
            ${{ moduleName | lcfirst }}To{{ dependentModule }}ServiceBridge = new \{{ organization }}\Yves\{{ moduleName }}\Dependency\Service\{{ moduleName }}To{{ dependentModule }}ServiceBridge(
                $container->getLocator()->{{ dependentModule | lcfirst }}()->service()
            );

            return ${{ moduleName | lcfirst }}To{{ dependentModule }}ServiceBridge;
        };

        return $container;
    }
