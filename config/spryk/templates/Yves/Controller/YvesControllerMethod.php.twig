
    /**
    * @param \Symfony\Component\HttpFoundation\Request $request
    *
    * @return \Spryker\Yves\Kernel\View\View|\Symfony\Component\HttpFoundation\RedirectResponse
    */
    public function {{ controllerMethod }}(\Symfony\Component\HttpFoundation\Request $request)
    {
        $response = $this->execute{{ controllerMethod | ucfirst }}($request);

        if (!is_array($response)) {
            return $response;
        }

        return $this->view(
            $response,
            [],
            '@{{ module }}/views/{{ templateName }}/{{ templateName }}.twig'
        );
    }

    /**
    * @param \Symfony\Component\HttpFoundation\Request $request
    *
    * @return array|\Symfony\Component\HttpFoundation\RedirectResponse
    */
    protected function execute{{ controllerMethod | ucfirst }}(\Symfony\Component\HttpFoundation\Request $request)
    {
        return [

        ];
    }
