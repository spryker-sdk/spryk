<?php

{{ include('Partials/license.twig') }}

namespace {{ organization }}\Glue\{{ module }}\Dependency\Client;

class {{ module }}To{{ dependentModule }}ClientBridge implements {{ module }}To{{ dependentModule }}ClientInterface
{
    /**
     * @var \{{ organization }}\Client\{{ dependentModule }}\{{ dependentModule }}ClientInterface
     */
    protected ${{ dependentModule | camelBack }}Client;

    /**
     * @param \{{ organization }}\Client\{{ dependentModule }}\{{ dependentModule }}ClientInterface ${{ dependentModule | camelBack }}Client
     */
    public function __construct(${{ dependentModule | camelBack }}Client)
    {
        $this->{{ dependentModule | camelBack }}Client = ${{ dependentModule | camelBack }}Client;
    }
}
