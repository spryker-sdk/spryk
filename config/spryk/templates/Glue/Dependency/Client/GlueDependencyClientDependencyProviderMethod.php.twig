
    /**
     * @return \Spryker\Glue\Kernel\Container
     */
    protected function {{ providerMethod }}(\Spryker\Glue\Kernel\Container $container): \Spryker\Glue\Kernel\Container
    {
        $container->set(static::CLIENT_{{ dependentModule | underscored | upper }}, function (\Spryker\Glue\Kernel\Container $container) {
            return new \{{ organization }}\Glue\{{ module }}\Dependency\Client\{{ module }}To{{ dependentModule }}ClientBridge(
                $container->getLocator()->{{ dependentModule | lcfirst }}()->client()
            );
        });

        return $container;
    }
