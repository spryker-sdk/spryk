

    /**
     * @Glue({
     *     "delete": {
     *          "summary": [
     *              "Removes {{ resourceType }}."
     *          ],
     *          "responses": {
     *              "404": "{{ resourceType | dashToCamelCase }} not found."
     *          }
     *     }
     * })
     *
     * @param \Spryker\Glue\GlueApplication\Rest\Request\Data\RestRequestInterface $restRequest
     *
     * @return \Spryker\Glue\GlueApplication\Rest\JsonApi\RestResponseInterface
     */
    public function {{ method }}(\Spryker\Glue\GlueApplication\Rest\Request\Data\RestRequestInterface $restRequest): \Spryker\Glue\GlueApplication\Rest\JsonApi\RestResponseInterface
    {
        return $this->getFactory()
            ->create{{ modelName }}{{ modelSuffix }}()
            ->delete{{ modelName }}($restRequest);
    }
